---
import SubSectionTitle from './sub-section-title.astro'

interface Props {
  title: string
  description: string
  icon: string
}
const props = Astro.props
---

<SubSectionTitle icon={props.icon} title={props.title}>
  <p class="text-lg inview-2 md:text-xl">
    {props.description}
  </p>
  <div class="inview-3">
    <slot />
  </div>
</SubSectionTitle>
